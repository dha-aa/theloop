import { OpenAICompatibleProvider } from "./openai-compatible.js";
export class OpenRouterProvider extends OpenAICompatibleProvider { public constructor(apiKey = process.env.OPENROUTER_API_KEY, baseUrl = process.env.OPENROUTER_BASE_URL ?? "https://openrouter.ai/api/v1") { super({ id: "openrouter", displayName: "OpenRouter", apiKey, baseUrl, defaultModel: process.env.OPENROUTER_MODEL ?? "openai/gpt-4o-mini", extraHeaders: { ...(process.env.OPENROUTER_SITE_URL ? { "HTTP-Referer": process.env.OPENROUTER_SITE_URL } : {}), ...(process.env.OPENROUTER_APP_NAME ? { "X-Title": process.env.OPENROUTER_APP_NAME } : {}) } }); } }
